'use strict';

/**
 * no-of-apparels-seleted router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::no-of-apparels-seleted.no-of-apparels-seleted');
