'use strict';

/**
 * no-of-apparels-seleted controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::no-of-apparels-seleted.no-of-apparels-seleted');
